import { StyleSheet, ViewStyle } from 'react-native';
import { STYLE_DIMENSIONS } from '@/Styles';

const styles = StyleSheet.create({
  carouselWrapper: {
    width: STYLE_DIMENSIONS.screenWidth,
  } as ViewStyle,
});

export default styles;
